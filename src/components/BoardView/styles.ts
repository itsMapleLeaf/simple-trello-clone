import styled from 'styled-components'

export const Title = styled.h2`
  margin-bottom: 1rem;
`

export const TaskLists = styled.div`
  display: flex;
  flex-wrap: wrap;

  margin: -0.5rem;

  > * {
    margin: 0.5rem;
  }
`
